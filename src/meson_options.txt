option('plugin_dir',
  type        : 'string',
  value       : '',
  description : 'Ruta de instalación del .so del plugin. Vacío = autodetectar (multilib si existe, /usr/lib/xfce4/panel/plugins si no).',
)
